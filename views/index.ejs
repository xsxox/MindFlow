<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MindFlow - 首页</title>
    <!-- 引入 Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- 引入 Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        .card { transition: transform 0.2s; }
        .card:hover { transform: translateY(-3px); }
        .cover-img { height: 200px; object-fit: cover; }
    </style>
</head>
<body class="bg-light">

    <!-- ================= 导航栏 ================= -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top mb-4 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">MindFlow</a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="navbar-nav ms-auto d-flex align-items-center gap-2">
                    
                    <!-- 1. 搜索框 -->
                    <form action="/articles/search" method="GET" class="d-flex me-2">
                        <div class="input-group input-group-sm">
                            <input class="form-control" type="search" name="q" placeholder="搜索文章..." required>
                            <button class="btn btn-secondary" type="submit"><i class="bi bi-search"></i></button>
                        </div>
                    </form>

                    <!-- 2. 用户状态判断 -->
                    <% if (currentUser) { %>
                        <!-- 个人中心入口 -->
                        <a href="/auth/profile" class="text-decoration-none me-2">
                            <span class="badge bg-secondary border border-secondary text-light p-2">
                                <i class="bi bi-person-fill"></i> 
                                <%= isAdmin ? '管理员: ' : '' %><%= currentUser %>
                            </span>
                        </a>
                        <!-- 写文章按钮 -->
                        <a href="/articles/new" class="btn btn-success btn-sm">
                            <i class="bi bi-pencil-square"></i> 写文章
                        </a>
                        <!-- 退出按钮 -->
                        <a href="/auth/logout" class="btn btn-outline-light btn-sm">退出</a>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-outline-light btn-sm">登录</a>
                        <a href="/auth/register" class="btn btn-primary btn-sm">注册</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <!-- ================= 头部欢迎语 ================= -->
        <div class="text-center py-4 mb-4">
            <h2 class="fw-light display-5">思维流淌的地方</h2>
            <p class="text-muted lead">分享技术、生活与灵感</p>
        </div>

        <!-- ================= 文章列表循环 ================= -->
        <div class="row g-4">
            <% articles.forEach(article => { %>
                <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm border-0 overflow-hidden">
                        
                        <!-- ★★★ [新增] 封面图片显示逻辑 ★★★ -->
                        <% if (article.cover) { %>
                            <img src="<%= article.cover %>" class="card-img-top cover-img" alt="文章封面">
                        <% } else { %>
                            <!-- 如果没有封面，显示一个灰色的占位条，保持对齐（可选） -->
                            <div class="bg-light border-bottom" style="height: 10px;"></div>
                        <% } %>

                        <div class="card-body d-flex flex-column">
                            <!-- 标题 -->
                            <h4 class="card-title h5">
                                <a href="/articles/<%= article.id %>" class="text-decoration-none text-dark fw-bold stretched-link">
                                    <%= article.title %>
                                </a>
                            </h4>
                            
                            <!-- 信息栏：作者 + 时间 -->
                            <div class="text-muted small mb-3 d-flex align-items-center">
                                <span class="me-2">
                                    <i class="bi bi-person"></i> 
                                    <%= article.author ? article.author.username : '匿名' %>
                                </span>
                                <span>
                                    <i class="bi bi-clock"></i> 
                                    <%= article.createdAt.toLocaleDateString() %>
                                </span>
                            </div>

                            <!-- 简介 -->
                            <p class="card-text text-secondary small flex-grow-1">
                                <%= article.description || '暂无简介...' %>
                            </p>
                            
                            <!-- 底部按钮栏 -->
                            <div class="mt-3 d-flex justify-content-between align-items-center" style="position: relative; z-index: 2;">
                                <a href="/articles/<%= article.id %>" class="btn btn-outline-primary btn-sm rounded-pill px-3">
                                    阅读全文
                                </a>

                                <!-- ★★★ 管理员删除按钮 ★★★ -->
                                <% if (isAdmin) { %>
                                    <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" onsubmit="return confirm('管理员操作：确定要强制删除这篇文章吗？');">
                                        <button type="submit" class="btn btn-outline-danger btn-sm border-0" title="删除">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </form>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>

            <!-- 空状态处理 -->
            <% if (articles.length === 0) { %>
                <div class="col-12 text-center py-5 bg-white rounded shadow-sm">
                    <i class="bi bi-inbox display-4 text-muted"></i>
                    <p class="text-muted mt-3">暂时还没有文章，快去发布第一篇吧！</p>
                    <% if (currentUser) { %>
                        <a href="/articles/new" class="btn btn-primary btn-lg mt-2">开始创作</a>
                    <% } %>
                </div>
            <% } %>
        </div>
    </div>

    <!-- ================= 底部版权 ================= -->
   <footer class="text-center text-muted py-5 mt-5 border-top bg-white">
        <div class="container">
            <p class="mb-2">&copy; 2026 MindFlow Blog System.</p>
            <div>
                <a href="/about" class="text-decoration-none text-secondary me-3">关于本站</a>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>