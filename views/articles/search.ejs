<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>搜索结果 - MindFlow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <style>
        /* === 1. 全局背景 (保持一致) === */
        body {
            background: linear-gradient(120deg, #fdfbfb 0%, #ebedee 100%);
            min-height: 100vh;
            color: #444;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        }

        /* === 2. 玻璃面板 === */
        .glass-panel {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.6);
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        }

        /* === 3. 导航栏样式 === */
        .navbar-custom {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(0,0,0,0.05);
            padding: 12px 0;
        }
        .brand-gradient {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 800;
        }

        /* === 4. 文章卡片 (复用首页样式) === */
        .article-card {
            border: none;
            overflow: hidden;
            border-radius: 16px;
            background: white;
            box-shadow: 0 2px 15px rgba(0,0,0,0.03);
            transition: transform 0.3s ease;
            height: 100%; /* 等高 */
        }
        .article-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
        }

        .card-img-wrap {
            height: 180px; 
            overflow: hidden;
            position: relative;
            background-color: #f8f9fa;
        }
        .card-img-wrap img {
            width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease;
        }
        .article-card:hover .card-img-wrap img { transform: scale(1.05); }

        .default-placeholder {
            width: 100%; height: 100%;
            background: linear-gradient(135deg, #e0c3fc 0%, #8ec5fc 100%); 
            display: flex; align-items: center; justify-content: center; opacity: 0.8;
        }
        .default-placeholder i { font-size: 4rem; color: white; opacity: 0.5; }

        .card-content {
            padding: 20px;
            background: linear-gradient(to bottom, rgba(255,255,255,0.9), #ffffff);
        }

        /* 标签小徽章 */
        .meta-badge {
            font-size: 0.75rem; padding: 4px 8px; background: #f0f2f5; color: #666;
            border-radius: 6px; margin-right: 8px; display: inline-flex; align-items: center; gap: 4px;
        }

        /* 按钮 */
        .btn-gradient-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none; color: white; transition: all 0.3s ease;
        }
        .btn-gradient-primary:hover {
            opacity: 0.9; color: white; transform: translateY(-2px);
        }
    </style>
</head>
<body>

    <!-- 导航栏 -->
    <nav class="navbar navbar-expand-lg navbar-light navbar-custom sticky-top mb-5">
        <div class="container">
            <a class="navbar-brand brand-gradient d-flex align-items-center gap-2" href="/">
                <i class="bi bi-wind"></i> MindFlow
            </a>
            <div class="ms-auto">
                <a href="/" class="btn btn-outline-dark btn-sm rounded-pill px-3">
                    <i class="bi bi-arrow-left"></i> 返回首页
                </a>
            </div>
        </div>
    </nav>

    <div class="container pb-5">
        
        <!-- 搜索结果头部 -->
        <div class="glass-panel p-4 mb-5 d-flex justify-content-between align-items-center">
            <div>
                <h4 class="fw-bold mb-1">搜索结果</h4>
                <div class="text-muted">
                    关键词 <span class="text-primary fw-bold">"<%= query %>"</span> 
                    共找到 <span class="badge bg-dark rounded-pill"><%= articles.length %></span> 篇
                </div>
            </div>
            <div>
                <!-- 右侧放个装饰性的大图标 -->
                <i class="bi bi-search text-secondary opacity-25" style="font-size: 2.5rem;"></i>
            </div>
        </div>

        <!-- 结果列表 -->
        <div class="row g-4">
            
            <% if (articles.length > 0) { %>
                
                <% articles.forEach(article => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="article-card">
                            
                            <!-- 封面逻辑 -->
                            <div class="card-img-wrap">
                                <% if (article.cover) { %>
                                    <img src="<%= article.cover %>" alt="Cover">
                                <% } else { %>
                                    <div class="default-placeholder"><i class="bi bi-fonts"></i></div>
                                <% } %>
                            </div>
                            
                            <!-- 内容 -->
                            <div class="card-content">
                                <h3 class="h5 fw-bold mb-2">
                                    <a href="/articles/<%= article.id %>" class="text-decoration-none text-dark stretched-link">
                                        <%= article.title %>
                                    </a>
                                </h3>
                                
                                <p class="text-secondary small mb-3 text-truncate">
                                    <%= article.description || '点击阅读全文...' %>
                                </p>

                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <span class="meta-badge"><i class="bi bi-person"></i> <%= article.author ? article.author.username : '匿名' %></span>
                                        <span class="meta-badge"><i class="bi bi-clock"></i> <%= article.createdAt.toLocaleDateString() %></span>
                                    </div>
                                    <!-- 这里的爱心只做展示，不交互，简化逻辑 -->
                                    <span class="small text-muted"><i class="bi bi-heart-fill text-danger opacity-50"></i> <%= article.likes.length %></span>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>

            <% } else { %>
                
                <!-- 空状态美化 -->
                <div class="col-12">
                    <div class="glass-panel p-5 text-center">
                        <i class="bi bi-emoji-frown display-1 text-muted opacity-25 mb-3"></i>
                        <h3 class="fw-bold text-muted">没有找到相关内容</h3>
                        <p class="text-secondary mb-4">换个关键词试试？或者去看看别的文章吧。</p>
                        <a href="/" class="btn btn-gradient-primary rounded-pill px-4">回到首页</a>
                    </div>
                </div>

            <% } %>

        </div>
    </div>

</body>
</html>