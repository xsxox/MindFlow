<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <title>搜索: <%= query %> - MindFlow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light container mt-4">

    <div class="mb-4">
        <a href="/" class="btn btn-outline-secondary">&larr; 返回首页</a>
    </div>

    <h3>Searching...</h3>
    <p class="text-muted">关键词 "<strong><%= query %></strong>" 的搜索结果：</p>

    <div class="row mt-4">
        <% if (articles.length > 0) { %>
            <% articles.forEach(article => { %>
                <div class="col-md-12 mb-3">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title"><%= article.title %></h5>
                            <p class="card-text text-muted"><%= article.description %></p>
                            <a href="/articles/<%= article.id %>" class="btn btn-primary btn-sm">阅读全文</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="col-12">
                <div class="alert alert-warning">
                    没有找到相关文章，换个词试试？
                </div>
            </div>
        <% } %>
    </div>

</body>
</html>