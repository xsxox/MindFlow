<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <title>编辑文章 - MindFlow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
    <h1 class="mb-4">编辑文章</h1>
    
    <!-- 注意：这里用了 ?_method=PUT 来模拟 PUT 请求 -->
    <form action="/articles/<%= article.id %>?_method=PUT" method="POST">
        
        <div class="mb-3">
            <label class="form-label">标题</label>
            <!-- value 属性把原来的标题填进去 -->
            <input type="text" name="title" value="<%= article.title %>" required class="form-control">
        </div>
        
        <div class="mb-3">
            <label class="form-label">简介</label>
            <textarea name="description" class="form-control" rows="2"><%= article.description %></textarea>
        </div>
        
        <div class="mb-3">
            <label class="form-label">内容 (Markdown)</label>
            <!-- textarea 的内容写在标签中间 -->
            <textarea name="markdown" required class="form-control" rows="15"><%= article.markdown %></textarea>
        </div>
        
        <a href="/articles/<%= article.id %>" class="btn btn-secondary">取消</a>
        <button type="submit" class="btn btn-primary">保存修改</button>
    </form>
</body>
</html>