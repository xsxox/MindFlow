<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>个人中心 - MindFlow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <!-- 导航栏 -->
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">MindFlow</a>
            <div class="navbar-nav ms-auto d-flex flex-row gap-3">
                <a href="/articles/new" class="btn btn-success btn-sm">+ 写文章</a>
                <a href="/auth/logout" class="btn btn-outline-light btn-sm">退出登录</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row">
            <!-- 左侧用户信息卡片 -->
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm text-center p-4">
                    <div class="mb-3">
                        <!-- 头像占位符 -->
                        <div class="bg-primary text-white rounded-circle d-inline-flex align-items-center justify-content-center" style="width: 80px; height: 80px; font-size: 32px; font-weight: bold;">
                            <%= currentUser ? currentUser.charAt(0).toUpperCase() : 'U' %>
                        </div>
                    </div>
                    <h4><%= currentUser %></h4>
                    <p class="text-muted mb-3">MindFlow 认证作者</p>
                    
                    <% if (isAdmin) { %>
                        <span class="badge bg-danger mb-3">超级管理员</span>
                    <% } %>

                    <hr>
                    <div class="d-flex justify-content-around text-center">
                        <div>
                            <h5><%= articles.length %></h5>
                            <small class="text-muted">已发布</small>
                        </div>
                        <div>
                            <h5>0</h5>
                            <small class="text-muted">获赞</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 右侧文章管理列表 -->
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                        <h5 class="m-0">我的文章管理</h5>
                        <span class="badge bg-secondary"><%= articles.length %> 篇</span>
                    </div>
                    <div class="card-body p-0">
                        <% if (articles.length > 0) { %>
                            <div class="list-group list-group-flush">
                                <% articles.forEach(article => { %>
                                    <div class="list-group-item d-flex justify-content-between align-items-center p-3">
                                        <div class="ms-2 overflow-hidden">
                                            <h6 class="mb-1 text-truncate">
                                                <a href="/articles/<%= article.id %>" class="text-decoration-none text-dark fw-bold">
                                                    <%= article.title %>
                                                </a>
                                            </h6>
                                            <small class="text-muted">
                                                发布于: <%= article.createdAt.toLocaleDateString() %>
                                            </small>
                                        </div>
                                        
                                        <!-- ★★★ 操作按钮组 ★★★ -->
                                        <div class="d-flex gap-2 align-items-center">
                                            <!-- 编辑按钮 -->
                                            <a href="/articles/edit/<%= article.id %>" class="btn btn-outline-primary btn-sm text-nowrap">
                                                编辑
                                            </a>
                                            
                                            <!-- 删除按钮 -->
                                            <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" onsubmit="return confirm('确定要删除这篇文章吗？');">
                                                <button type="submit" class="btn btn-outline-danger btn-sm text-nowrap">删除</button>
                                            </form>
                                        </div>
                                    </div>
                                <% }) %>
                            </div>
                        <% } else { %>
                            <div class="text-center py-5">
                                <p class="text-muted mb-3">空空如也，去分享你的第一个想法吧！</p>
                                <a href="/articles/new" class="btn btn-primary">开始写作</a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>